<?xml version="1.0" encoding="utf-8"?>
<FrameLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:orientation="vertical"
    android:paddingStart="@dimen/row_padding_start"
    android:paddingEnd="@dimen/row_padding_end"
    android:layout_width="match_parent"
    android:layout_height="@dimen/row_double_height"
    android:background="#123">

    <ImageView
        android:id="@+id/contact_photo"
        android:layout_width="@dimen/row_photo_icon_dimen"
        android:layout_height="@dimen/row_photo_icon_dimen"
        android:layout_gravity="center_vertical"
        android:contentDescription="@string/contact_photo"
        tools:src="@android:drawable/presence_online"/>

    <TextView
        android:id="@+id/contact"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textColor="@color/primary"
        android:textSize="@dimen/font_size_normal"
        android:paddingTop="@dimen/row_text_padding_top_first"
        android:layout_marginStart="@dimen/row_content_margin_start"
        tools:text="John Smith"/>

    <TextView
        android:id="@+id/preview"
        android:layout_width="fill_parent"
        android:layout_height="wrap_content"
        android:textSize="@dimen/font_size_secondary"
        android:paddingTop="@dimen/row_text_padding_top_second"
        android:layout_marginStart="@dimen/row_content_margin_start"
        android:singleLine="true"
        android:ellipsize="end"
        tools:text="Hi! I'm here waiting outside. Come meet me at the store around the block."/>

    <TextView
        android:id="@+id/time_ago"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="top|end"
        android:paddingTop="@dimen/row_text_padding_top_first"
        tools:text="1 min"/>

</FrameLayout>